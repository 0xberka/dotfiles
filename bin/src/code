#!/bin/bash

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run your command
#nohup "$SCRIPT_DIR/vscode/code" "$@" >/dev/null 2>&1 &
setsid "$SCRIPT_DIR/vscode/code" "$@" >/dev/null 2>&1 < /dev/null &
